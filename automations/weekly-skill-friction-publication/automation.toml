version = 1
id = "weekly-skill-friction-publication"
kind = "cron"
name = "Weekly Skill Friction Publication"
prompt = "Use $daily-skill-friction to run Weekly publication preparation from the last completed Daily state. Depend on the host-owned combined prefetch at Friday 06:30, then run `/Users/hoteng/.local/share/uv/python/cpython-3.14.2-macos-aarch64-none/bin/python3.14 /Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace/.codex-local/daily-skill-friction/repos/codex-host-workflows/scripts/host_setup.py doctor --weekly --no-launchctl`; require JSON `status: ready` including `freshness-final-rebind` and the current `weekly-pair-receipt`, keep the 60-minute age limit, and never add `--historical`. Process only cases selected by Joey, prepare one verified signed local ledger commit per case, finalize one exact binding-and-scope publication manifest, then stop; the manifest is not publish approval. Do not wait for Daily, implement repairs, fetch, push, open pull requests, merge, close publication state, or replace an existing pending-publication commit."
status = "PAUSED"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=FR;BYHOUR=7;BYMINUTE=0"
model = "gpt-5.6-sol"
reasoning_effort = "xhigh"
execution_environment = "worktree"
cwds = ["/Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace"]
