version = 1
id = "daily-skill-friction"
kind = "cron"
name = "Daily Skill Friction"
prompt = "Use $daily-skill-friction to run the weekday Daily audit in host-local staging mode. Depend on the host-owned 02:45 `prefetch-control`, then run `/Users/hoteng/.local/share/uv/python/cpython-3.14.2-macos-aarch64-none/bin/python3.14 -I -B -S /Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace/.codex-local/daily-skill-friction/repos/codex-host-workflows/scripts/host_setup.py doctor --no-launchctl`; require JSON `status: ready` including `freshness-final-rebind`, and never add `--historical`. Then audit the deduplicated active and archived session corpus, evaluate currentness and repair placement, and write only verified local staging state. Complete with the exact seven nonnegative summary counts `candidates_considered`, `cases_created`, `cases_updated`, `cases_unchanged`, `cases_dormant`, `no_issue_observations`, and `blocked_actions`, plus `next_watchpoint` as null or a bounded string. Make no repository or GitHub writes, return a compact receipt when nothing changed, and do not repair any case."
status = "PAUSED"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=3;BYMINUTE=10"
model = "gpt-5.6-sol"
reasoning_effort = "xhigh"
execution_environment = "worktree"
cwds = ["/Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace"]
